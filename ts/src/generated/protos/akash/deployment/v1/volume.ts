import { isSet } from "../../../../../encoding/typeEncodingHelpers.ts"
import type { DeepPartial, MessageFns } from "../../../../../encoding/typeEncodingHelpers.ts"
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: akash/deployment/v1/volume.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Duration } from "../../../google/protobuf/duration.ts";

/** VolumeRef addresses one Storage entry of a storage-only ("volume") group. */
export interface VolumeRef {
  /**
   * Owner is the account bech32 address of the user who owns the volume group.
   * It is a string representing a valid bech32 account address.
   *
   * Example:
   *   "akash1..."
   */
  owner: string;
  /** Dseq (deployment sequence number) of the volume deployment. */
  dseq: bigint;
  /**
   * Gseq (group sequence number) of the volume group. Always 1 in v1:
   * a deployment containing a volume group contains only that group.
   */
  gseq: number;
  /** Name is the Storage.Name of the single storage entry in the volume group. */
  name: string;
}

/**
 * VolumePolicy marks a group as a storage-only volume group and carries its
 * lifecycle contract. Presence of this message IS the volume discriminator.
 */
export interface VolumePolicy {
  /**
   * Vid is the tenant-chosen data-continuity label (DNS-label grammar,
   * [a-z0-9]([-a-z0-9]{0,61}[a-z0-9])?). Typed field -- NEVER a storage
   * attribute (attributes flow into AttributesSubsetOf provider matching
   * and would make the order unbiddable).
   */
  vid: string;
  /** Reclaim declares the post-close fate of the volume's data. */
  reclaim: VolumePolicy_ReclaimPolicy;
  /**
   * Retention is the post-close window the provider must keep the data
   * adoptable. Bounded by deployment Params.MaxVolumeRetention; priced into
   * the bid. GC deadline is chain-computable: closedLease.ClosedAt + retention.
   */
  retention:
    | Duration
    | undefined;
  /**
   * MaxAttachments bounds concurrent attached compute leases.
   * Validation forces 1 in v1 (RWO); the field exists so RWX is additive.
   */
  maxAttachments: number;
  /**
   * MaxReplicas declares how many replica volumes the tenant may point at
   * this volume (replica_of backrefs). >0 obliges the winning provider to
   * serve replication export traffic -- a bid-time-known, priced duty.
   */
  maxReplicas: number;
  /**
   * Adopt, when set, makes this volume an ADOPTION of a dead retained volume:
   * same owner, same vid, group closed, within its retention window. Bids are
   * chain-gated to the provider of adopt's last closed lease.
   */
  adopt:
    | VolumeRef
    | undefined;
  /**
   * ReplicaOf, when set, declares this volume a replica of the named volume
   * (same owner). Drives data-plane authorization on both ends.
   */
  replicaOf: VolumeRef | undefined;
}

/**
 * ReclaimPolicy declares what happens to the volume's data after its
 * lease closes.
 */
export enum VolumePolicy_ReclaimPolicy {
  /** invalid - Prefix should start with 0 in enum. So declaring dummy state. */
  invalid = 0,
  /** retain - retain keeps the data adoptable for the retention window after close. */
  retain = 1,
  /** delete - delete destroys the data immediately after close and detach. */
  delete = 2,
  UNRECOGNIZED = -1,
}

export function volumePolicy_ReclaimPolicyFromJSON(object: any): VolumePolicy_ReclaimPolicy {
  switch (object) {
    case 0:
    case "invalid":
      return VolumePolicy_ReclaimPolicy.invalid;
    case 1:
    case "retain":
      return VolumePolicy_ReclaimPolicy.retain;
    case 2:
    case "delete":
      return VolumePolicy_ReclaimPolicy.delete;
    case -1:
    case "UNRECOGNIZED":
    default:
      return VolumePolicy_ReclaimPolicy.UNRECOGNIZED;
  }
}

export function volumePolicy_ReclaimPolicyToJSON(object: VolumePolicy_ReclaimPolicy): string {
  switch (object) {
    case VolumePolicy_ReclaimPolicy.invalid:
      return "invalid";
    case VolumePolicy_ReclaimPolicy.retain:
      return "retain";
    case VolumePolicy_ReclaimPolicy.delete:
      return "delete";
    case VolumePolicy_ReclaimPolicy.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

function createBaseVolumeRef(): VolumeRef {
  return { owner: "", dseq: 0n, gseq: 0, name: "" };
}

export const VolumeRef: MessageFns<VolumeRef, "akash.deployment.v1.VolumeRef"> = {
  $type: "akash.deployment.v1.VolumeRef" as const,

  encode(message: VolumeRef, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.owner !== "") {
      writer.uint32(10).string(message.owner);
    }
    if (message.dseq !== 0n) {
      if (BigInt.asUintN(64, message.dseq) !== message.dseq) {
        throw new globalThis.Error("value provided for field message.dseq of type uint64 too large");
      }
      writer.uint32(16).uint64(message.dseq);
    }
    if (message.gseq !== 0) {
      writer.uint32(24).uint32(message.gseq);
    }
    if (message.name !== "") {
      writer.uint32(34).string(message.name);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VolumeRef {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVolumeRef();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.owner = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.dseq = reader.uint64() as bigint;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.gseq = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.name = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VolumeRef {
    return {
      owner: isSet(object.owner) ? globalThis.String(object.owner) : "",
      dseq: isSet(object.dseq) ? BigInt(object.dseq) : 0n,
      gseq: isSet(object.gseq) ? globalThis.Number(object.gseq) : 0,
      name: isSet(object.name) ? globalThis.String(object.name) : "",
    };
  },

  toJSON(message: VolumeRef): unknown {
    const obj: any = {};
    if (message.owner !== "") {
      obj.owner = message.owner;
    }
    if (message.dseq !== 0n) {
      obj.dseq = message.dseq.toString();
    }
    if (message.gseq !== 0) {
      obj.gseq = Math.round(message.gseq);
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    return obj;
  },
  fromPartial(object: DeepPartial<VolumeRef>): VolumeRef {
    const message = createBaseVolumeRef();
    message.owner = object.owner ?? "";
    message.dseq = (object.dseq !== undefined && object.dseq !== null) ? BigInt(object.dseq) : 0n;
    message.gseq = object.gseq ?? 0;
    message.name = object.name ?? "";
    return message;
  },
};

function createBaseVolumePolicy(): VolumePolicy {
  return {
    vid: "",
    reclaim: 0,
    retention: undefined,
    maxAttachments: 0,
    maxReplicas: 0,
    adopt: undefined,
    replicaOf: undefined,
  };
}

export const VolumePolicy: MessageFns<VolumePolicy, "akash.deployment.v1.VolumePolicy"> = {
  $type: "akash.deployment.v1.VolumePolicy" as const,

  encode(message: VolumePolicy, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.vid !== "") {
      writer.uint32(10).string(message.vid);
    }
    if (message.reclaim !== 0) {
      writer.uint32(16).int32(message.reclaim);
    }
    if (message.retention !== undefined) {
      Duration.encode(message.retention, writer.uint32(26).fork()).join();
    }
    if (message.maxAttachments !== 0) {
      writer.uint32(32).uint32(message.maxAttachments);
    }
    if (message.maxReplicas !== 0) {
      writer.uint32(40).uint32(message.maxReplicas);
    }
    if (message.adopt !== undefined) {
      VolumeRef.encode(message.adopt, writer.uint32(50).fork()).join();
    }
    if (message.replicaOf !== undefined) {
      VolumeRef.encode(message.replicaOf, writer.uint32(58).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VolumePolicy {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVolumePolicy();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.vid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.reclaim = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.retention = Duration.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.maxAttachments = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.maxReplicas = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.adopt = VolumeRef.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.replicaOf = VolumeRef.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VolumePolicy {
    return {
      vid: isSet(object.vid) ? globalThis.String(object.vid) : "",
      reclaim: isSet(object.reclaim) ? volumePolicy_ReclaimPolicyFromJSON(object.reclaim) : 0,
      retention: isSet(object.retention) ? Duration.fromJSON(object.retention) : undefined,
      maxAttachments: isSet(object.max_attachments) ? globalThis.Number(object.max_attachments) : 0,
      maxReplicas: isSet(object.max_replicas) ? globalThis.Number(object.max_replicas) : 0,
      adopt: isSet(object.adopt) ? VolumeRef.fromJSON(object.adopt) : undefined,
      replicaOf: isSet(object.replica_of) ? VolumeRef.fromJSON(object.replica_of) : undefined,
    };
  },

  toJSON(message: VolumePolicy): unknown {
    const obj: any = {};
    if (message.vid !== "") {
      obj.vid = message.vid;
    }
    if (message.reclaim !== 0) {
      obj.reclaim = volumePolicy_ReclaimPolicyToJSON(message.reclaim);
    }
    if (message.retention !== undefined) {
      obj.retention = Duration.toJSON(message.retention);
    }
    if (message.maxAttachments !== 0) {
      obj.max_attachments = Math.round(message.maxAttachments);
    }
    if (message.maxReplicas !== 0) {
      obj.max_replicas = Math.round(message.maxReplicas);
    }
    if (message.adopt !== undefined) {
      obj.adopt = VolumeRef.toJSON(message.adopt);
    }
    if (message.replicaOf !== undefined) {
      obj.replica_of = VolumeRef.toJSON(message.replicaOf);
    }
    return obj;
  },
  fromPartial(object: DeepPartial<VolumePolicy>): VolumePolicy {
    const message = createBaseVolumePolicy();
    message.vid = object.vid ?? "";
    message.reclaim = object.reclaim ?? 0;
    message.retention = (object.retention !== undefined && object.retention !== null)
      ? Duration.fromPartial(object.retention)
      : undefined;
    message.maxAttachments = object.maxAttachments ?? 0;
    message.maxReplicas = object.maxReplicas ?? 0;
    message.adopt = (object.adopt !== undefined && object.adopt !== null)
      ? VolumeRef.fromPartial(object.adopt)
      : undefined;
    message.replicaOf = (object.replicaOf !== undefined && object.replicaOf !== null)
      ? VolumeRef.fromPartial(object.replicaOf)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

type _unused_DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function _unused_isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface _unused_MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
