import { isSet } from "../../../../../encoding/typeEncodingHelpers.ts"
import type { DeepPartial, MessageFns } from "../../../../../encoding/typeEncodingHelpers.ts"
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: akash/deployment/v1beta5/params.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Coin } from "../../../cosmos/base/v1beta1/coin.ts";
import { Duration } from "../../../google/protobuf/duration.ts";

/** Params defines the parameters for the x/deployment module. */
export interface Params {
  /** MinDeposits holds a list of the minimum amount of deposits for each a coin. */
  minDeposits: Coin[];
  /**
   * MaxVolumeSize is the maximum size in bytes of the single storage entry
   * of a volume group. Applies to volume groups ONLY; compute groups keep
   * the compiled per-unit storage cap.
   */
  maxVolumeSize: bigint;
  /**
   * MaxVolumeRetention bounds VolumePolicy.retention: the longest post-close
   * window a tenant may ask a provider to keep volume data adoptable.
   */
  maxVolumeRetention:
    | Duration
    | undefined;
  /**
   * MaxVolumeReplicas bounds VolumePolicy.max_replicas: the most replica
   * volumes a tenant may point at one volume.
   */
  maxVolumeReplicas: number;
}

function createBaseParams(): Params {
  return { minDeposits: [], maxVolumeSize: 0n, maxVolumeRetention: undefined, maxVolumeReplicas: 0 };
}

export const Params: MessageFns<Params, "akash.deployment.v1beta5.Params"> = {
  $type: "akash.deployment.v1beta5.Params" as const,

  encode(message: Params, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.minDeposits) {
      Coin.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.maxVolumeSize !== 0n) {
      if (BigInt.asUintN(64, message.maxVolumeSize) !== message.maxVolumeSize) {
        throw new globalThis.Error("value provided for field message.maxVolumeSize of type uint64 too large");
      }
      writer.uint32(16).uint64(message.maxVolumeSize);
    }
    if (message.maxVolumeRetention !== undefined) {
      Duration.encode(message.maxVolumeRetention, writer.uint32(26).fork()).join();
    }
    if (message.maxVolumeReplicas !== 0) {
      writer.uint32(32).uint32(message.maxVolumeReplicas);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Params {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseParams();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.minDeposits.push(Coin.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.maxVolumeSize = reader.uint64() as bigint;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.maxVolumeRetention = Duration.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.maxVolumeReplicas = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Params {
    return {
      minDeposits: globalThis.Array.isArray(object?.min_deposits)
        ? object.min_deposits.map((e: any) => Coin.fromJSON(e))
        : [],
      maxVolumeSize: isSet(object.max_volume_size) ? BigInt(object.max_volume_size) : 0n,
      maxVolumeRetention: isSet(object.max_volume_retention)
        ? Duration.fromJSON(object.max_volume_retention)
        : undefined,
      maxVolumeReplicas: isSet(object.max_volume_replicas) ? globalThis.Number(object.max_volume_replicas) : 0,
    };
  },

  toJSON(message: Params): unknown {
    const obj: any = {};
    if (message.minDeposits?.length) {
      obj.min_deposits = message.minDeposits.map((e) => Coin.toJSON(e));
    }
    if (message.maxVolumeSize !== 0n) {
      obj.max_volume_size = message.maxVolumeSize.toString();
    }
    if (message.maxVolumeRetention !== undefined) {
      obj.max_volume_retention = Duration.toJSON(message.maxVolumeRetention);
    }
    if (message.maxVolumeReplicas !== 0) {
      obj.max_volume_replicas = Math.round(message.maxVolumeReplicas);
    }
    return obj;
  },
  fromPartial(object: DeepPartial<Params>): Params {
    const message = createBaseParams();
    message.minDeposits = object.minDeposits?.map((e) => Coin.fromPartial(e)) || [];
    message.maxVolumeSize = (object.maxVolumeSize !== undefined && object.maxVolumeSize !== null) ? BigInt(object.maxVolumeSize) : 0n;
    message.maxVolumeRetention = (object.maxVolumeRetention !== undefined && object.maxVolumeRetention !== null)
      ? Duration.fromPartial(object.maxVolumeRetention)
      : undefined;
    message.maxVolumeReplicas = object.maxVolumeReplicas ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

type _unused_DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function _unused_isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface _unused_MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
