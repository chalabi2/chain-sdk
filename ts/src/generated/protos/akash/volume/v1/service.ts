import { bytesFromBase64, base64FromBytes, isSet } from "../../../../../encoding/typeEncodingHelpers.ts"
import type { DeepPartial, MessageFns } from "../../../../../encoding/typeEncodingHelpers.ts"
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: akash/volume/v1/service.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

/**
 * ExportRequest asks the exporting provider for a volume image stream.
 * Authorization is derived from chain state on both ends: the requester must
 * be the destination provider of a live migration/replication for the named
 * volume, and it must match the mTLS client certificate (x/cert identity).
 */
export interface ExportRequest {
  /** owner is the account bech32 address of the volume owner. */
  owner: string;
  /** vid is the tenant-chosen data-continuity label of the volume. */
  vid: string;
  /** dseq is the volume deployment on the EXPORTER's side. */
  dseq: bigint;
  /**
   * requester is the destination provider address; it must match the mTLS
   * client certificate presented on the connection.
   */
  requester: string;
  /**
   * from_snapshot is the diff base snapshot id. Empty requests a full
   * export; non-empty requests an incremental diff (resumable).
   */
  fromSnapshot: string;
  /**
   * resume_offset is the byte offset to resume a previously interrupted
   * export stream from.
   */
  resumeOffset: bigint;
}

/** ExportChunk is one verifiable chunk of the exported volume image stream. */
export interface ExportChunk {
  /** data is the raw image bytes of this chunk. */
  data: Uint8Array;
  /** sha256 is the digest of this chunk's data. */
  sha256: Uint8Array;
  /** offset is the byte offset of this chunk within the export stream. */
  offset: bigint;
}

/**
 * StatusRequest queries the exporting provider for the transfer state of a
 * volume. Authorization mirrors ExportRequest.
 */
export interface StatusRequest {
  /** owner is the account bech32 address of the volume owner. */
  owner: string;
  /** vid is the tenant-chosen data-continuity label of the volume. */
  vid: string;
  /** dseq is the volume deployment on the EXPORTER's side. */
  dseq: bigint;
  /**
   * requester is the destination provider address; it must match the mTLS
   * client certificate presented on the connection.
   */
  requester: string;
}

/**
 * StatusResponse reports sync lag, held snapshots, and the whole-image
 * digest so the importer can verify a completed transfer.
 */
export interface StatusResponse {
  /**
   * snapshots lists the snapshot ids held for this volume, oldest first.
   * Any of them may serve as a diff base for an incremental export.
   */
  snapshots: string[];
  /** latest_snapshot is the most recent snapshot id. */
  latestSnapshot: string;
  /** size is the size in bytes of the full image at the latest snapshot. */
  size: bigint;
  /** sha256 is the whole-image digest at the latest snapshot. */
  sha256: Uint8Array;
  /**
   * sync_lag_seconds is the age of the latest snapshot relative to the
   * live volume: how far behind a destination syncing from it would be.
   */
  syncLagSeconds: bigint;
}

function createBaseExportRequest(): ExportRequest {
  return { owner: "", vid: "", dseq: 0n, requester: "", fromSnapshot: "", resumeOffset: 0n };
}

export const ExportRequest: MessageFns<ExportRequest, "akash.volume.v1.ExportRequest"> = {
  $type: "akash.volume.v1.ExportRequest" as const,

  encode(message: ExportRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.owner !== "") {
      writer.uint32(10).string(message.owner);
    }
    if (message.vid !== "") {
      writer.uint32(18).string(message.vid);
    }
    if (message.dseq !== 0n) {
      if (BigInt.asUintN(64, message.dseq) !== message.dseq) {
        throw new globalThis.Error("value provided for field message.dseq of type uint64 too large");
      }
      writer.uint32(24).uint64(message.dseq);
    }
    if (message.requester !== "") {
      writer.uint32(34).string(message.requester);
    }
    if (message.fromSnapshot !== "") {
      writer.uint32(42).string(message.fromSnapshot);
    }
    if (message.resumeOffset !== 0n) {
      if (BigInt.asUintN(64, message.resumeOffset) !== message.resumeOffset) {
        throw new globalThis.Error("value provided for field message.resumeOffset of type uint64 too large");
      }
      writer.uint32(48).uint64(message.resumeOffset);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExportRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExportRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.owner = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.vid = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.dseq = reader.uint64() as bigint;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.requester = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.fromSnapshot = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.resumeOffset = reader.uint64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExportRequest {
    return {
      owner: isSet(object.owner) ? globalThis.String(object.owner) : "",
      vid: isSet(object.vid) ? globalThis.String(object.vid) : "",
      dseq: isSet(object.dseq) ? BigInt(object.dseq) : 0n,
      requester: isSet(object.requester) ? globalThis.String(object.requester) : "",
      fromSnapshot: isSet(object.from_snapshot) ? globalThis.String(object.from_snapshot) : "",
      resumeOffset: isSet(object.resume_offset) ? BigInt(object.resume_offset) : 0n,
    };
  },

  toJSON(message: ExportRequest): unknown {
    const obj: any = {};
    if (message.owner !== "") {
      obj.owner = message.owner;
    }
    if (message.vid !== "") {
      obj.vid = message.vid;
    }
    if (message.dseq !== 0n) {
      obj.dseq = message.dseq.toString();
    }
    if (message.requester !== "") {
      obj.requester = message.requester;
    }
    if (message.fromSnapshot !== "") {
      obj.from_snapshot = message.fromSnapshot;
    }
    if (message.resumeOffset !== 0n) {
      obj.resume_offset = message.resumeOffset.toString();
    }
    return obj;
  },
  fromPartial(object: DeepPartial<ExportRequest>): ExportRequest {
    const message = createBaseExportRequest();
    message.owner = object.owner ?? "";
    message.vid = object.vid ?? "";
    message.dseq = (object.dseq !== undefined && object.dseq !== null) ? BigInt(object.dseq) : 0n;
    message.requester = object.requester ?? "";
    message.fromSnapshot = object.fromSnapshot ?? "";
    message.resumeOffset = (object.resumeOffset !== undefined && object.resumeOffset !== null) ? BigInt(object.resumeOffset) : 0n;
    return message;
  },
};

function createBaseExportChunk(): ExportChunk {
  return { data: new Uint8Array(0), sha256: new Uint8Array(0), offset: 0n };
}

export const ExportChunk: MessageFns<ExportChunk, "akash.volume.v1.ExportChunk"> = {
  $type: "akash.volume.v1.ExportChunk" as const,

  encode(message: ExportChunk, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.data.length !== 0) {
      writer.uint32(10).bytes(message.data);
    }
    if (message.sha256.length !== 0) {
      writer.uint32(18).bytes(message.sha256);
    }
    if (message.offset !== 0n) {
      if (BigInt.asUintN(64, message.offset) !== message.offset) {
        throw new globalThis.Error("value provided for field message.offset of type uint64 too large");
      }
      writer.uint32(24).uint64(message.offset);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExportChunk {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExportChunk();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.data = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sha256 = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.offset = reader.uint64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExportChunk {
    return {
      data: isSet(object.data) ? bytesFromBase64(object.data) : new Uint8Array(0),
      sha256: isSet(object.sha256) ? bytesFromBase64(object.sha256) : new Uint8Array(0),
      offset: isSet(object.offset) ? BigInt(object.offset) : 0n,
    };
  },

  toJSON(message: ExportChunk): unknown {
    const obj: any = {};
    if (message.data.length !== 0) {
      obj.data = base64FromBytes(message.data);
    }
    if (message.sha256.length !== 0) {
      obj.sha256 = base64FromBytes(message.sha256);
    }
    if (message.offset !== 0n) {
      obj.offset = message.offset.toString();
    }
    return obj;
  },
  fromPartial(object: DeepPartial<ExportChunk>): ExportChunk {
    const message = createBaseExportChunk();
    message.data = object.data ?? new Uint8Array(0);
    message.sha256 = object.sha256 ?? new Uint8Array(0);
    message.offset = (object.offset !== undefined && object.offset !== null) ? BigInt(object.offset) : 0n;
    return message;
  },
};

function createBaseStatusRequest(): StatusRequest {
  return { owner: "", vid: "", dseq: 0n, requester: "" };
}

export const StatusRequest: MessageFns<StatusRequest, "akash.volume.v1.StatusRequest"> = {
  $type: "akash.volume.v1.StatusRequest" as const,

  encode(message: StatusRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.owner !== "") {
      writer.uint32(10).string(message.owner);
    }
    if (message.vid !== "") {
      writer.uint32(18).string(message.vid);
    }
    if (message.dseq !== 0n) {
      if (BigInt.asUintN(64, message.dseq) !== message.dseq) {
        throw new globalThis.Error("value provided for field message.dseq of type uint64 too large");
      }
      writer.uint32(24).uint64(message.dseq);
    }
    if (message.requester !== "") {
      writer.uint32(34).string(message.requester);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StatusRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStatusRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.owner = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.vid = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.dseq = reader.uint64() as bigint;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.requester = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StatusRequest {
    return {
      owner: isSet(object.owner) ? globalThis.String(object.owner) : "",
      vid: isSet(object.vid) ? globalThis.String(object.vid) : "",
      dseq: isSet(object.dseq) ? BigInt(object.dseq) : 0n,
      requester: isSet(object.requester) ? globalThis.String(object.requester) : "",
    };
  },

  toJSON(message: StatusRequest): unknown {
    const obj: any = {};
    if (message.owner !== "") {
      obj.owner = message.owner;
    }
    if (message.vid !== "") {
      obj.vid = message.vid;
    }
    if (message.dseq !== 0n) {
      obj.dseq = message.dseq.toString();
    }
    if (message.requester !== "") {
      obj.requester = message.requester;
    }
    return obj;
  },
  fromPartial(object: DeepPartial<StatusRequest>): StatusRequest {
    const message = createBaseStatusRequest();
    message.owner = object.owner ?? "";
    message.vid = object.vid ?? "";
    message.dseq = (object.dseq !== undefined && object.dseq !== null) ? BigInt(object.dseq) : 0n;
    message.requester = object.requester ?? "";
    return message;
  },
};

function createBaseStatusResponse(): StatusResponse {
  return { snapshots: [], latestSnapshot: "", size: 0n, sha256: new Uint8Array(0), syncLagSeconds: 0n };
}

export const StatusResponse: MessageFns<StatusResponse, "akash.volume.v1.StatusResponse"> = {
  $type: "akash.volume.v1.StatusResponse" as const,

  encode(message: StatusResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.snapshots) {
      writer.uint32(10).string(v!);
    }
    if (message.latestSnapshot !== "") {
      writer.uint32(18).string(message.latestSnapshot);
    }
    if (message.size !== 0n) {
      if (BigInt.asUintN(64, message.size) !== message.size) {
        throw new globalThis.Error("value provided for field message.size of type uint64 too large");
      }
      writer.uint32(24).uint64(message.size);
    }
    if (message.sha256.length !== 0) {
      writer.uint32(34).bytes(message.sha256);
    }
    if (message.syncLagSeconds !== 0n) {
      if (BigInt.asIntN(64, message.syncLagSeconds) !== message.syncLagSeconds) {
        throw new globalThis.Error("value provided for field message.syncLagSeconds of type int64 too large");
      }
      writer.uint32(40).int64(message.syncLagSeconds);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StatusResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStatusResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.snapshots.push(reader.string());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.latestSnapshot = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.size = reader.uint64() as bigint;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.sha256 = reader.bytes();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.syncLagSeconds = reader.int64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StatusResponse {
    return {
      snapshots: globalThis.Array.isArray(object?.snapshots)
        ? object.snapshots.map((e: any) => globalThis.String(e))
        : [],
      latestSnapshot: isSet(object.latest_snapshot) ? globalThis.String(object.latest_snapshot) : "",
      size: isSet(object.size) ? BigInt(object.size) : 0n,
      sha256: isSet(object.sha256) ? bytesFromBase64(object.sha256) : new Uint8Array(0),
      syncLagSeconds: isSet(object.sync_lag_seconds) ? BigInt(object.sync_lag_seconds) : 0n,
    };
  },

  toJSON(message: StatusResponse): unknown {
    const obj: any = {};
    if (message.snapshots?.length) {
      obj.snapshots = message.snapshots;
    }
    if (message.latestSnapshot !== "") {
      obj.latest_snapshot = message.latestSnapshot;
    }
    if (message.size !== 0n) {
      obj.size = message.size.toString();
    }
    if (message.sha256.length !== 0) {
      obj.sha256 = base64FromBytes(message.sha256);
    }
    if (message.syncLagSeconds !== 0n) {
      obj.sync_lag_seconds = message.syncLagSeconds.toString();
    }
    return obj;
  },
  fromPartial(object: DeepPartial<StatusResponse>): StatusResponse {
    const message = createBaseStatusResponse();
    message.snapshots = object.snapshots?.map((e) => e) || [];
    message.latestSnapshot = object.latestSnapshot ?? "";
    message.size = (object.size !== undefined && object.size !== null) ? BigInt(object.size) : 0n;
    message.sha256 = object.sha256 ?? new Uint8Array(0);
    message.syncLagSeconds = (object.syncLagSeconds !== undefined && object.syncLagSeconds !== null) ? BigInt(object.syncLagSeconds) : 0n;
    return message;
  },
};

function _unused_bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function _unused_base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

type _unused_DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function _unused_isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface _unused_MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
